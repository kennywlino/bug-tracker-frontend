import { Header as mantineHeader } from '@mantine/core';

const Header = () => {
  return(
    <mantineHeader
      height={60} 
      p="lg" 
    >
      <h1>VFT</h1>
    </mantineHeader>
  );
};

export default Header;
